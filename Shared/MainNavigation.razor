<Navbar Background=appService.Settings.NavbarBackground Direction=AppService.Settings.NavbarDirection class="navbar navbar-expand-md navbar-dark navbar-vertical">

    <a href="/" class="navbar-brand navbar-brand-autodark">
        <img src="/images/logo.png" alt="Book Store" class="navbar-brand-image me-2">
        <h2 class="text-muted mb-0">Book Store</h2>
    </a>

    <NavbarMenu>
        @* <AuthorizeView Roles="@Constants.Role_Administrators"> *@
            <NavbarMenuItem Href="/" Text="Dashboard">
                <MenuItemIcon>
                    <Icon class="icon" Elements="@AppIcons.Dashboard" />
                </MenuItemIcon>
            </NavbarMenuItem>

            <NavbarMenuItem Text="Books" Href="/books">
                <MenuItemIcon>
                    <Icon class="icon" Elements="@AppIcons.Book" />
                </MenuItemIcon>
            </NavbarMenuItem>

            <NavbarMenuItem Text="Stores" Href="/stores">
                <MenuItemIcon>
                    <Icon class="icon" Elements="@AppIcons.Building_Store" />
                </MenuItemIcon>
            </NavbarMenuItem>

            <NavbarMenuItem Text="Orders" Href="/orders">
                <MenuItemIcon>
                    <Icon class="icon" Elements="@AppIcons.Building_Store" />
                </MenuItemIcon>
            </NavbarMenuItem>

        @* </AuthorizeView> *@

    </NavbarMenu>

</Navbar>